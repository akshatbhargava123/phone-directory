import React from 'react';
import styled from 'styled-components';

const Header = styled.div`
  background: black;
  color: white;
  text-align: center;
  padding: 10px;
  font-size: 20px;
`

export default () => (
  <Header>
    Phone Directory
  </Header>
);
